services:
  - type: web
    name: jbistro-flask
    runtime: python
    rootDir: flask-app
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
